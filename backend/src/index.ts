/**
 * 後端進入點（佔位）— 全端三層：api=表現層、application=應用邏輯層、infrastructure=資料層
 * @see docs/ARCHITECTURE.md
 */
export function createServerPlaceholder() {
  return {
    status: 'not_implemented',
    message: 'BlindBox API server — 請依 ARCHITECTURE.md 實作',
  };
}
