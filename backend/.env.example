# PostgreSQL 連線（Supabase：Dashboard → Project Settings → Database → Connection string）
# 建議 migrations 使用 Direct connection 或 Session pooler（port 5432 / 6543）
# 格式：postgresql+psycopg2://postgres.[ref]:[PASSWORD]@...supabase.com:5432/postgres
DATABASE_URL=

# 亦可沿用專案根目錄 .env（alembic 會自動載入）
